get-configs:
  call: http.post
  args:
    url: http://localhost:8086/services/endpoints/configs/currency-get_euro_exchange-test-configs
    body:
      params: ${incoming.body.params}
      headers: ${incoming.body.headers}
      body: ${incoming.body.body}
  result: configs
return_value:
  wrapper: false
  return: ${configs.response.body}
