declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'ACTIVE-SERVICES'"
  method: get
  accepts: json
  returns: json
  namespace: service

get_services_list:
  call: http.get
  args:
    url: "[#SERVICE_RESQL]/service/list_active_services"
  result: results

return_ok:
  status: 200
  wrapper: false
  return: ${results.response.body}
  next: end
