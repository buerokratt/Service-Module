get_ruuter_secrets:
  call: http.get
  args:
    url: http://node_server:3008/ruuter/secrets
  result: results

return_ok:
  status: 200
  wrapper: false
  return: ${results.response.body}
  next: end
